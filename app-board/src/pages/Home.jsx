import React from 'react';
import MenuBar from '../components/MenuBar';
import { Outlet } from 'react-router';

function Home(props) {
    return (
        <div>
            <h1>메인 페이지</h1>
        </div>
    );
}

export default Home;